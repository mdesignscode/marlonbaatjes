export const slideTransition = (delay: number = 0, x: number = 100) => ({ x, delay, duration: 1000 });
export const flyTransition = (delay: number = 0, y: number = 50) => ({ y, delay, duration: 1000 });

